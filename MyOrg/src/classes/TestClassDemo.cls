@isTest
private class TestClassDemo {
    static testMethod void test() {
        /*Declare the variables, Fetch the required records, Create and update records*/
        Test.startTest();
        /*Call the controller for code coverage*/
        Test.stopTest();
    }
}